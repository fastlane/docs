machine:
  python:
    version: pypy-2.2.1
  ruby:
    version: 2.3.0
dependencies:
  pre:
    - pip install mkdocs
test:
  override:
    - mkdocs build
    - bundle exec danger
deployment:
  master:
    branch: master
    commands:
      - ./scripts/ci/deploy.sh
