machine:
  python:
    version: pypy-2.2.1
dependencies:
  pre:
    - pip install mkdocs
test:
  override:
    - echo "YOLO"
deployment:
  master:
    branch: master
    commands:
      - ./scripts/ci/deploy.sh
